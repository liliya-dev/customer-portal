import { LayoutMemo as Layout } from '../../layout/Layout';
import { SupportSceneMemo as Support } from '../../scenes/SupportScene';
export default function Page() {
  return (
    <Layout>
      <Support />
    </Layout>
  );
}
